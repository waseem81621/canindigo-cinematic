/**
 * Phase 5.5 placeholder — real content lands in Phase 6.
 * Routes are scaffolded so that link wiring + scroll-restoration can
 * be verified before any content work begins.
 */
export function InteriorsPage() {
  return (
    <main className="min-h-screen flex items-center justify-center px-6">
      <div className="text-center max-w-[640px]">
        <span className="block text-[11px] uppercase tracking-[0.18em] text-indigo-accent mb-4">
          Division 02
        </span>
        <h1 className="font-display text-5xl md:text-6xl text-text-primary tracking-tight">
          Interiors — coming in Phase 6
        </h1>
        <p className="mt-5 text-[15px] text-text-muted">
          Route placeholder. Full division content (split-screen hero, four
          trade cards, bundle CTA) lands in the next phase.
        </p>
      </div>
    </main>
  );
}
